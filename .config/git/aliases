; vim: set filetype=gitconfig:
[alias]
    adog = log --all --decorate --oneline --graph
    amend = commit --amend --no-edit
    br = branch
    cdf = clean -df
    ci = commit
    co = checkout
    last = log -1 HEAD
    pr = pull --rebase
    rh = reset HEAD
    st = status -s
    sta = status
